[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    e2e: browser-based end-to-end tests that require a running Streamlit app
    slow: tests that may be slower than regular unit tests
